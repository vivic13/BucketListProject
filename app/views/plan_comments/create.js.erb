<% if @comment.save  %>

	$("#comment-list").prepend("<%=j render(:partial => 'plan_comments/comment', :locals => {:c => @comment })%>");
	$("#count").text('<%= @plan.comments.count %>');

	$("#comment-text").val("");
	$("#image-text").val("");

  $(".upload-preview img").attr("src","");
<% end %>