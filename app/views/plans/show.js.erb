
var html = "<%= j( render :partial => "list" ) %>";
$("#comment-list").append(html);


<% if @next_page %>
 $("#load-more").attr("href", "<%= plan_path(@plan,:page => @next_page) %>")
<% else %>
 $("#load-more").remove();
<% end %>