<%= form_for [@plan, @plan.photos.new] , html: { multipart: true } , :remote => true   do |f| %> 
	<%= f.file_field :image ,:id =>"photo-text" %>
  <%= f.submit "上傳更多圖片", class:"btn-little"%> 
<% end %>